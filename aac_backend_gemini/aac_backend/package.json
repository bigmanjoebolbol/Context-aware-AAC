{
  "name": "aac-suggestion-backend",
  "version": "1.0.0",
  "description": "Minimal backend that turns AAC fallback requests into short reply suggestions via the Gemini API.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
